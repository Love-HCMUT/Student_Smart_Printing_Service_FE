// The configs folder contains config files where we store environment variables.
// We will use this folder to setup configurations in your application.
// Besides you can use it to stores constant variables, then we just change a place, and they will apply for all placed that used.
